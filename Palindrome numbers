def palindroome(num):
    return str(num) == str(num)[::-1]

palindrome_numbers = []

for i in range(100, 201):
    
    if palindroome(i):
        
        palindrome_numbers.append(i)

print("Palindrome numbers between 100 and 200 are:",palindrome_numbers)
